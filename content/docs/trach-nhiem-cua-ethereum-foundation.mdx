---
title: Trách nhiệm của Ethereum Foundation
description: Văn bản ghi nhớ trách nhiệm của Ethereum Foundation với Ethereum
---

<div className="flex flex-col gap-0 m-0 p-0 text-[0] leading-0 [&_img]:block [&_img]:w-full [&_img]:h-auto [&_img]:m-0 [&_img]:p-0 [&_img]:border-0 [&_img]:align-top">
  {Array.from({ length: 38 }, (_, i) => i + 1).map((n) => (
    <img
      key={n}
      src={`/ef-mandate/${n}.jpg`}
      alt={`EF Mandate -Trang ${n}`}
    />
  ))}
</div>
